# Data Directory

`data/sources/` is for local source files supplied by the project owner after license review. These files are ignored by Git so restricted or unreviewed content is not committed accidentally.

`data/fixtures/` is only for tiny test fixtures. Fixture content must be labeled test-only and must never be treated as production Quran text, translation, or tafsir.
