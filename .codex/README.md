# .codex Notes

The main prompt is duplicated at `.codex/prompts/quran-e2e.md` so it can be reused as a prompt-library file.

The repository-level `AGENTS.md` at the project root is the primary persistent instruction file for Codex. Keep it short and update it when recurring mistakes happen.
